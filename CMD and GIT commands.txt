dir -->> to list all the files in the directory
mkdir -->> to make a new directory
cd "directory name" -->> to move into the nested directory
cls -->> to clear cmd
ctrl c -->> to stop Browser Sync

TO CHANGE From 'C' drive to 'D' drive:
cd\
cd windows
d:

GIT Commands

git clone https://github.com/Addanki-Hasith/Automate-Boring-Stuff-with-Python.git

git status
git add *
git add "Browser Sync Commands.txt"
git commit -m "Browser Sync commands are added"
git push